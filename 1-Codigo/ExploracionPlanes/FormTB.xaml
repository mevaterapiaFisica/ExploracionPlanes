<local:DialogoWpf x:Class="ExploracionPlanes.FormTB"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ExploracionPlanes"
        Title="FormTB" Width="360" SizeToContent="Height"
        WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Background="#F7F8FA" FontFamily="Segoe UI" FontSize="13">
    <Window.Resources>
        <Style x:Key="BotonPrimario" TargetType="Button">
            <Setter Property="Background" Value="#1B2A4A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="18,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="BotonSecundario" TargetType="Button" BasedOn="{StaticResource BotonPrimario}">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#1B2A4A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#1B2A4A" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="6" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="L_Texto" x:FieldModifier="public" Grid.Row="0" TextWrapping="Wrap" Text="label1" />
        <Grid Grid.Row="2">
            <TextBox x:Name="TB_Llenar" Padding="4" TextChanged="TB_Llenar_TextChanged" />
            <PasswordBox x:Name="PB_Llenar" Padding="4" Visibility="Collapsed" PasswordChanged="PB_Llenar_PasswordChanged" />
        </Grid>
        <CheckBox x:Name="CHB_Extra" x:FieldModifier="public" Grid.Row="4" Visibility="Collapsed" />
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BT_Cancelar" Content="Cancelar" Style="{StaticResource BotonSecundario}" IsCancel="True" Margin="0,0,10,0" />
            <Button x:Name="BT_Aceptar" Content="Aceptar" Style="{StaticResource BotonPrimario}" IsEnabled="False" Click="BT_Aceptar_Click" />
        </StackPanel>
    </Grid>
</local:DialogoWpf>
