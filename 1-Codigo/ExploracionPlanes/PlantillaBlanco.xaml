<local:DialogoWpf x:Class="ExploracionPlanes.PlantillaBlanco"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ExploracionPlanes"
        Title="Plantilla en blanco" Height="620" Width="780"
        WindowStartupLocation="CenterScreen"
        Background="#F7F8FA" FontFamily="Segoe UI" FontSize="13">
    <Window.Resources>
        <Style x:Key="BotonSecundario" TargetType="Button">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#1B2A4A" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#1B2A4A" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#9AA1AD" />
                    <Setter Property="BorderBrush" Value="#D5D9E0" />
                    <Setter Property="Background" Value="#F7F8FA" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1B2A4A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderBrush" Value="#DDE1E8" />
            <Setter Property="BorderThickness" Value="0,0,1,0" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="DGV_Analisis" Grid.Row="0" IsReadOnly="True" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="All"
                  HorizontalGridLinesBrush="#DDE1E8" VerticalGridLinesBrush="#DDE1E8"
                  RowHeight="30" AlternatingRowBackground="#F1F3F6" Background="White"
                  HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Estructura" Binding="{Binding Estructura}" Width="150" />
                <DataGridTextColumn x:Name="Col_Prioridad" Header="Prioridad" Binding="{Binding Prioridad}" Width="80" Visibility="Collapsed" />
                <DataGridTextColumn Header="Métrica" Binding="{Binding Metrica}" Width="110" />
                <DataGridTextColumn Header="Vol [cm3]" Binding="{Binding Volumen}" Width="90" />
                <DataGridTextColumn Header="En Plan" Binding="{Binding EnPlan}" Width="80" />
                <DataGridTextColumn Header="Esperado" Binding="{Binding Esperado}" Width="*" MinWidth="140" />
                <DataGridTextColumn Header="Ref." Binding="{Binding Referencia}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel x:Name="SP_Filtros" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel x:Name="SP_FiltroNumFx" Orientation="Vertical" Margin="0,0,20,0">
                <TextBlock Text="Filtrar por número de fracciones" FontWeight="SemiBold" Margin="0,0,0,4" />
                <ComboBox x:Name="CB_FiltroNumFx" Width="180"
                          SelectionChanged="CB_Filtro_SelectionChanged" />
            </StackPanel>
            <StackPanel x:Name="SP_FiltroVolPTV" Orientation="Vertical">
                <TextBlock Text="Filtrar por volumen de PTV" FontWeight="SemiBold" Margin="0,0,0,4" />
                <ComboBox x:Name="CB_FiltroVolPTV" Width="180"
                          SelectionChanged="CB_Filtro_SelectionChanged" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="BT_GuardarReporte" Content="Guardar como PDF" Style="{StaticResource BotonSecundario}" Margin="0,0,12,0" Click="BT_GuardarReporte_Click" />
            <Button x:Name="BT_Imprimir" Content="Imprimir" Style="{StaticResource BotonSecundario}" Click="BT_Imprimir_Click" />
        </StackPanel>
    </Grid>
</local:DialogoWpf>
